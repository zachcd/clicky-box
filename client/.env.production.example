# .env.production.example
# Copy this to client/.env.production and fill in your server URL.
# GitHub Actions reads VITE_SERVER_URL from repo Variables instead,
# so this file is only needed for manual `npm run build:client` deployments.

VITE_SERVER_URL=wss://your-railway-app.up.railway.app
VITE_BASE=/clicky-box/
